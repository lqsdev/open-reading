<div>
    <strong>
    <?php
    $url = $this->url('',array('controller' => 'article', 'action' => 'edit', 'bid' => $bid, 'cdid' => $cdid));
    $link = sprintf('<a href="%s" title="%s">%s</a>', $url, __('Add New Article'), __('Add New Article'));
    echo $link;
    ?>
    </strong>
</div>

<table class="table-striped table-condensed">
    <tr>
        <th class="span6"><?php echo __('Article Title'); ?></th>
        <th class="span3"><?php echo __('Operation'); ?></th>
    </tr>
    <?php
    foreach ($articles as $article)
    {
    ?>
    <tr>
        <td>
            <center><?php echo $article['title']; ?></center>
        </td>
        <td>
            <center>
                    <?php
                    $url = $this->url('', array('controller' => 'article',
                        'action' => 'edit',
                        'bid' => $bid,
                        'cdid' => $cdid,
                        'id' => $article['article_id']));
                    $link = sprintf('<a href="%s" title="%s">%s</a>', $url, __('Edit Article'), '<i class="icon-edit"></i>');
                    echo $link;
                    ?>
            </center>
        </td>
    </tr>
    <?php
    }
    ?>
</table>

<div class="span2">
    <strong>
        <?php
        $url = $this->url('', array('controller' => 'catalogue', 'action' => 'edit', 'bid' => $bid));
        $link = sprintf('<a href="%s" title="%s">%s</a>', $url, __('Edit Catalogue'), __('Edit Catalogue'));
        echo $link;
        ?>
    </strong>
</div>
